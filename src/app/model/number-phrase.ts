export class NumberPhrase {

    andable: boolean;

    commable: boolean;

    // the "three" in "three hundred" 
    cardinal: string;

    // the "hundred" in "three hundred" 
    numeral: string;

}
