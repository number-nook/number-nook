import { NumericalExpression } from './numerical-expression';

describe('NumericalExpression', () => {
  it('should create an instance', () => {
    expect(new NumericalExpression()).toBeTruthy();
  });
});
