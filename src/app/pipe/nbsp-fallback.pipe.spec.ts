import { NbspFallbackPipe } from './nbsp-fallback.pipe';

describe('NbspFallbackPipe', () => {
  it('create an instance', () => {
    const pipe = new NbspFallbackPipe();
    expect(pipe).toBeTruthy();
  });
});
